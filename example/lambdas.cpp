#include <sigslot/signal.hpp>
#include <iostream>

int main() {

    sigslot::signal<int> sig;

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });
    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    sig.connect([](int) {
        std::cout << "lambda\n";
    });

    return 0;
}
